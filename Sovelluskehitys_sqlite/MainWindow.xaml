<Window x:Class="Sovelluskehitys_sqlite.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sovelluskehitys_sqlite"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top buttons -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Initialize DB" Click="InitializeDb_Click" Margin="0,0,10,0"/>
            <Button Content="Load" Click="Load_Click" Margin="0,0,10,0"/>
            <Button Content="Add Sample" Click="AddSample_Click" Margin="0,0,10,0"/>
            <Button Content="Delete Selected" Click="DeleteSelected_Click"/>
        </StackPanel>

        <!-- Data grid -->
        <DataGrid x:Name="PeopleGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id"
                                    Binding="{Binding Id}"
                                    Width="SizeToHeader" />
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name}"
                                    Width="*" />
                <DataGridTextColumn Header="Age"
                                    Binding="{Binding Age}"
                                    Width="SizeToHeader" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status text -->
        <TextBlock x:Name="StatusText"
                   Grid.Row="2"
                   Margin="0,5,0,0"
                   Foreground="Gray"/>
    </Grid>
</Window>
